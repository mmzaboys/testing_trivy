name: Security

on:
  pull_request:
  push:
    branches: ["main"]

jobs:
  security:
    uses: mmzaboys/full_scan/.github/workflows/full-security-suite.yml@main
    with:
      run_iac: true
      run_secrets: true
      run_sast: false
      run_containers: true
